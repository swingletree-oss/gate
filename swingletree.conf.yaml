gate:
  port: 3020
  api:
    token:
  plugins:
    nebula:
      enabled: true
      base: http://plugin-nebula:3010
      insecure: false # disable authentication for this plugin endpoint
    sonarqube:
      enabled: true
      base: http://plugin-sonarqube:3020
      insecure: false # disable authentication for this plugin endpoint
    twistlock:
      enabled: true
      base: http://plugin-twistlock:3030
      insecure: false # disable authentication for this plugin endpoint
    zap:
      enabled: true
      base: http://plugin-zap:3040
      insecure: false # disable authentication for this plugin endpoint
    testng:
      enabled: true
      base: http://plugin-testng:3050
      insecure: false # disable authentication for this plugin endpoint
    junit:
      enabled: true
      base: http://plugin-junit:3060
      insecure: false # disable authentication for this plugin endpoint
